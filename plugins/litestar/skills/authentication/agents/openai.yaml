display_name: "Litestar Authentication"
short_description: "Implement Litestar authentication and authorization using custom authentication middleware, built-in security backends, guards, endpoint inclusion/exclusion rules, JWT backends, and secret handling."
default_prompt: "Use the authentication skill and follow its workflow for this Litestar task."
